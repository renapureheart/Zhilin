# 知林更新包：修复“保存失败”

这次更新修复了一个预览版错误：用 `http://公网 IP` 打开网站时，首次保存学习记录可能失败，并被错误地提示为“浏览器本地储存空间已满”。

## 上传到 GitHub

1. 解压本更新包。
2. 打开 GitHub 中的 `renapureheart/Zhilin` 仓库首页。
3. 点击 **Add file** → **Upload files**。
4. 把解压后的 `app` 文件夹和 `out` 文件夹拖进上传区域；保留原有文件夹结构。
5. 页面下方点击 **Commit changes**。

## 让服务器使用新版本

在阿里云的 Workbench 终端依次输入：

```bash
cd /opt/zhilin-public
sudo git pull
sudo cp -r /opt/zhilin-public/out/. /var/www/html/
sudo systemctl reload nginx
```

回到网站后按 `Ctrl + F5`（或完全关闭后重新打开网页）。现在应能正常保存学习记录。

## 说明

当前仍是预览版：学习记录只保存于当前浏览器，不会同步到别的设备。以后接入账号、数据库、域名和 HTTPS 后，可以再升级为可多人使用的正式版本。
