# 知林独立部署指南（中国大陆预览版）

## 你需要准备什么

1. 一个代码托管账号：GitHub、Gitee 或 GitLab 均可；
2. 一个服务器：阿里云或腾讯云的轻量应用服务器即可；
3. 一个域名（正式公开时再买也可以）；
4. 一台自己的电脑，用来上传代码和执行命令。

此预览版不需要数据库、短信服务、邮箱服务或账号系统。

## 第一次上线：推荐使用服务器 + Docker

### 1. 保存源代码

下载本项目压缩包，解压后上传到 GitHub 或 Gitee 的一个私有仓库。不要上传密码、云账号密钥或证件照片。

### 2. 购买服务器

在阿里云或腾讯云购买轻量应用服务器，系统选择 Ubuntu 24.04。创建时保存好登录方式；不要把服务器密码发给任何人。

### 3. 登录服务器并安装 Docker

在云厂商控制台打开“远程登录”或使用 SSH，执行：

```bash
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
```

重新登录一次服务器。

### 4. 把项目放到服务器

最简单的方法是在服务器执行：

```bash
git clone 你的代码仓库地址 zhilin
cd zhilin
docker build -t zhilin-preview .
docker run -d --name zhilin-preview --restart unless-stopped -p 80:80 zhilin-preview
```

在云服务器的安全组/防火墙中放行 TCP 80 端口。部署完成后，先在你自己可控的环境中检查页面是否正常显示；对外开放前继续完成域名、备案和 HTTPS 设置。

### 5. 使用域名和 HTTPS

在域名服务商处添加一条 A 记录，把域名指向服务器公网 IP。然后在服务器安装 Caddy 或 Nginx + Certbot 配置 HTTPS。不要把只用 IP 的测试地址长期公开。

## ICP 备案

如果网站部署在中国大陆服务器并面向公众提供服务，通常需要在云厂商处提交 ICP 备案。备案期间如何测试、何时可公开访问，以云厂商和当地通信管理局的要求为准；不要把未完成备案的地址作为正式公开链接传播。

## 以后怎样更新网站

每次我或开发者修改代码后：

```bash
git pull
docker build -t zhilin-preview .
docker rm -f zhilin-preview
docker run -d --name zhilin-preview --restart unless-stopped -p 80:80 zhilin-preview
```

先在自己的电脑运行 `npm run dev` 检查，再更新服务器。

## 从预览版升级为正式版

正式版要增加以下内容：

- 中国大陆手机号验证码登录；
- 后端 API 和数据库；
- 私有文件存储；
- 短信发送频率限制和反滥用保护；
- 数据备份、隐私说明和运营流程。

上线正式账户功能前，不要把浏览器本地数据当成用户正式数据，也不要收集手机号或其他个人信息。
